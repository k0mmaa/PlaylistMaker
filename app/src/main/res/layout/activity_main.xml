<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    tools:context=".MainActivity">


    <LinearLayout
        android:id="@+id/main_header"
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_height"
        android:background="@color/background"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/padding_medium">

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/title"
            android:textAppearance="@style/title"
            android:gravity="center_vertical" />
    </LinearLayout>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/container_height_weighted"
        android:layout_weight="@integer/container_weight"
        android:orientation="vertical"
        android:padding="@dimen/padding_medium"
        android:gravity="center">


        <!-- Gjbcr -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/search_button"
            android:layout_width="match_parent"
            android:layout_height="@dimen/button_height_weighted"
            android:layout_weight="@integer/button_weight"
            android:layout_marginBottom="@dimen/margin_medium"
            android:backgroundTint="@color/button_background"
            android:text="@string/search"
            android:textAppearance="@style/button_text"
            android:textColor="@color/text_color"
            app:cornerRadius="@dimen/button_corner_radius"
            app:icon="@drawable/ic_search_24"
            app:iconGravity="textStart"
            app:iconPadding="@dimen/icon_padding"
            app:iconTint="@color/icon_color" />

        <!-- Медиатека -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/media_button"
            android:layout_width="match_parent"
            android:layout_height="@dimen/button_height_weighted"
            android:layout_weight="@integer/button_weight"
            android:layout_marginBottom="@dimen/margin_medium"
            android:backgroundTint="@color/button_background"
            app:cornerRadius="@dimen/button_corner_radius"
            app:icon="@drawable/ic_media_24"
            app:iconGravity="textStart"
            app:iconPadding="@dimen/icon_padding"
            app:iconTint="@color/icon_color"
            android:text="@string/media"
            android:textAppearance="@style/button_text"
            android:textColor="@color/text_color"/>

        <!-- Настройки -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/settings_button"
            android:layout_width="match_parent"
            android:layout_height="@dimen/button_height_weighted"
            android:layout_weight="@integer/button_weight"
            android:backgroundTint="@color/button_background"
            app:cornerRadius="@dimen/button_corner_radius"
            app:icon="@drawable/ic_settings_24"
            app:iconGravity="textStart"
            app:iconPadding="@dimen/icon_padding"
            app:iconTint="@color/icon_color"
            android:text="@string/settings"
            android:textAppearance="@style/button_text"
            android:textColor="@color/text_color"/>
    </LinearLayout>

</LinearLayout>